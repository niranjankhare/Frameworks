use automation;

create table PAGES (
`PAGEID` int (10) NOT NULL AUTO_INCREMENT,
`PAGENAME` VARCHAR (50), -- SELECT
`PARENTID` VARCHAR (50), -- SELECT
`PAGEDESCRIPTION` VARCHAR (150), -- SELECT
PRIMARY KEY (`PAGEID`)
);

create table GUIMAP (
`GUIMAPID` int (10) NOT NULL AUTO_INCREMENT,
`PAGEID` int (10) NOT NULL, 
`ELEMENTTYPE` VARCHAR (50), -- SELECT (This is user friendly name for user)
`CONTROLNAME` VARCHAR (50), 
`CONTROLDESCRIPTION` VARCHAR (150),
`FIELDNAME` VARCHAR (150), -- GEnerated from type+name
 PRIMARY KEY (`GUIMAPID`),
 CONSTRAINT fk_page FOREIGN KEY (PAGEID)
 REFERENCES PAGES(`PAGEID`)
 );



create table PROPERTIES (
`PROPERTYID` int (10) NOT NULL AUTO_INCREMENT,
`GUIMAPID` int (10) NOT NULL, 
`STANDARDCLASS`  VARCHAR (50), -- same as element type from guimap
`MAPPEDCLASS`  VARCHAR (50), 
`LOCATORSTRATEGY` int (10) NOT NULL, -- Select
`TAGNAME` VARCHAR (50), -- SELECT
`ATTRNAME` VARCHAR (50), -- SELECT
`ATRRVALUE` VARCHAR (50), -- SELECT
`CUSTOMVALUE` VARCHAR (150), -- SELECT
PRIMARY KEY (`PROPERTYID`),
CONSTRAINT fk_guimap FOREIGN KEY (GUIMAPID)
  REFERENCES GUIMAP(`GUIMAPID`)
  ON DELETE CASCADE
  ON UPDATE CASCADE
);

CREATE VIEW automation.sample AS

SELECT PAGENAME, PARENTID,PAGEDESCRIPTION 
FROM
automation.PAGES;
/*
delimiter //
CREATE TRIGGER updateElements AFTER INSERT ON GUIMAP
FOR EACH ROW 
BEGIN 
INSERT INTO PROPERTIES  VALUES (new.idemployee.40);
   END//;
delimiter ;
*/

INSERT INTO SAMPLE
SET
`PAGENAME`= "Login",
`PARENTID`= null,
`PAGEDESCRIPTION`= "Heimdall Login";


SELECT * FROM PAGES;


